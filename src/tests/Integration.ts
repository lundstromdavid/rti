import { RTI } from "../RTI";

/* const User = RTI.create({
	id: RTI.string,
	username: RTI.string.range(8, 25), 
	password: RTI.string.range(8, 25),
	email: RTI.string,
	age: RTI.number,
	subscribesToNewsletter: RTI.optional.string
});

type TUser = typeof User;


 */